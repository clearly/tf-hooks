type : object
properties :
  defaults :
    type : object
    properties :
      target:
        type : string
        format : uri
  hooks :
    type : object
    properties :
      actions :
        type : array
        items :
          type : string
          enum :
            - add
      target:
        type : string
        format : uri
